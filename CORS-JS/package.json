{
  "name": "cors-js",
  "version": "1.0.0",
  "description": "estudo sobre CORS (compartilhamento de recursos de origem cruzada)",
  "main": "index.js",
  "scripts": {
    "build": "babel src -d dist --presets es2015",
    "watch": "babel src -w -d dist --presets es2015",
    "start": "npm run build && nodemon dist/server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.19.0",
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.3",
    "request": "^2.88.0"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.24.1",
    "nodemon": "^2.0.2"
  }
}
